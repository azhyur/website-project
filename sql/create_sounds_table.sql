CREATE TABLE IF NOT EXISTS sounds {
    id INT NOT NULL,
    name VARCHAR(256) NOT NULL,
    sound_type VARCHAR(256) NOT NULL,
    category VARCHAR(256) NOT NULL,
    filename VARCHAR(256) NOT NULL
};
